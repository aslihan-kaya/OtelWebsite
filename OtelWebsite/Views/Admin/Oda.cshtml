@model IEnumerable<OtelWebsite.Models.Odalar>

@{
    ViewBag.Title = "Oda";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<h2>Oda</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OdaFiyat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YatakSayisi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OdaTuru)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Servisler.ServisAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OdaFiyat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YatakSayisi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OdaTuru)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Servisler.ServisAdi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OdaNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.OdaNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OdaNo })
        </td>
    </tr>
}

</table>
